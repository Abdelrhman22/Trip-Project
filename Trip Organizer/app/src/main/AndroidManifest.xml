<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="eg.com.iti.triporganizer">
    <!-- Arafa start -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" /> <!-- Arafa End -->
    <!-- Mai start -->
    <!-- Mai End -->
    <!-- Salma start -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".screens.history.HistoryActivity"/>
        <activity android:name=".screens.dialog.DialogActivity" />
        <activity android:name=".screens.addTrip.AddTripActivity" >

        </activity>

        <activity
            android:name=".screens.home.HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" /> <!-- Arafa Start -->
        <activity
            android:name=".screens.splash.SplashActivity"
            android:configChanges="orientation"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBLPdtvo4KoNgMDdVYXcCtJOpM8yvkuSCQ" /> <!-- Arafa End *************************************************** -->
        <!-- Mai Start -->
        <activity android:name=".screens.login.LoginActivity"></activity> <!-- Mai End *************************************************** -->
        <!-- salma start -->
        <activity android:name=".screens.register.RegistrationActivity" />
    </application>

</manifest>